---
// Datos estáticos del footer
const currentYear = new Date().getFullYear();
const socialLinks = [
  { name: "GitHub", url: "https://github.com/leidi", icon: "github" },
  { name: "LinkedIn", url: "https://linkedin.com/in/leidi", icon: "linkedin" },
  { name: "Twitter", url: "https://twitter.com/leidi", icon: "twitter" },
  { name: "Email", url: "mailto:hola@leidi.dev", icon: "email" }
];
---

<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-main">
        <div class="footer-brand">
          <h3 class="footer-title">Leidi</h3>
          <p class="footer-description">
            Desarrolladora Frontend apasionada por crear experiencias web increíbles.
          </p>
        </div>
        
        <div class="footer-links">
          <div class="footer-section">
            <h4 class="footer-section-title">Navegación</h4>
            <ul class="footer-nav">
              <li><a href="#inicio">Inicio</a></li>
              <li><a href="#sobre-mi">Sobre mí</a></li>
              <li><a href="#proyectos">Proyectos</a></li>
              <li><a href="#skills">Skills</a></li>
              <li><a href="#contacto">Contacto</a></li>
            </ul>
          </div>
          
          <div class="footer-section">
            <h4 class="footer-section-title">Conecta conmigo</h4>
            <div class="social-links">
              {socialLinks.map((link) => (
                <a href={link.url} class="social-link" title={link.name} target="_blank" rel="noopener noreferrer">
                  <span class="social-icon" data-icon={link.icon}></span>
                </a>
              ))}
            </div>
          </div>
        </div>
      </div>
      
      <div class="footer-bottom">
        <div class="footer-copyright">
          <p>&copy; {currentYear} Leidi. Todos los derechos reservados.</p>
        </div>
        <div class="footer-tech">
          <p>Hecho con <span class="heart">❤️</span> y <strong>Astro</strong></p>
        </div>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: linear-gradient(135deg, var(--color-bg-secondary) 0%, var(--color-bg-tertiary) 100%);
    padding: var(--space-3xl) 0 var(--space-xl) 0;
    margin-top: var(--space-3xl);
    border-top: 1px solid rgba(0, 0, 0, 0.1);
  }
  
  .footer-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
  }
  
  .footer-main {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3xl);
  }
  
  .footer-brand {
    max-width: 400px;
  }
  
  .footer-title {
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--color-primary);
    margin-bottom: var(--space-md);
  }
  
  .footer-description {
    color: var(--color-text-secondary);
    line-height: 1.6;
  }
  
  .footer-links {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-xl);
  }
  
  .footer-section-title {
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin-bottom: var(--space-md);
  }
  
  .footer-nav {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }
  
  .footer-nav a {
    color: var(--color-text-secondary);
    text-decoration: none;
    transition: color var(--transition-fast);
  }
  
  .footer-nav a:hover {
    color: var(--color-primary);
  }
  
  .social-links {
    display: flex;
    gap: var(--space-md);
  }
  
  .social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: var(--border-radius-lg);
    background: var(--color-bg-primary);
    color: var(--color-text-secondary);
    text-decoration: none;
    transition: all var(--transition-fast);
    box-shadow: var(--shadow-sm);
  }
  
  .social-link:hover {
    background: var(--color-primary);
    color: white;
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }
  
  .social-icon {
    font-size: 1.25rem;
  }
  
  .social-icon[data-icon="github"]::before { content: "🐙"; }
  .social-icon[data-icon="linkedin"]::before { content: "💼"; }
  .social-icon[data-icon="twitter"]::before { content: "🐦"; }
  .social-icon[data-icon="email"]::before { content: "📧"; }
  
  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: var(--space-xl);
    border-top: 1px solid rgba(0, 0, 0, 0.1);
  }
  
  .footer-copyright,
  .footer-tech {
    color: var(--color-text-light);
    font-size: 0.875rem;
  }
  
  .heart {
    color: #ef4444;
    animation: heartbeat 1.5s ease-in-out infinite;
  }
  
  @keyframes heartbeat {
    0%, 50%, 100% {
      transform: scale(1);
    }
    25%, 75% {
      transform: scale(1.1);
    }
  }
  
  /* Responsive */
  @media (max-width: 768px) {
    .footer-main {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
      text-align: center;
    }
    
    .footer-links {
      grid-template-columns: 1fr;
      gap: var(--space-lg);
    }
    
    .footer-bottom {
      flex-direction: column;
      gap: var(--space-md);
      text-align: center;
    }
  }
  
  /* Tema oscuro */
  @media (prefers-color-scheme: dark) {
    .footer {
      border-top-color: rgba(255, 255, 255, 0.1);
    }
    
    .footer-bottom {
      border-top-color: rgba(255, 255, 255, 0.1);
    }
    
    .social-link {
      background: var(--color-bg-tertiary);
    }
  }
</style>
